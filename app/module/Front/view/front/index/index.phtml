<?php foreach ($this->results as $result): ?>
    <?php
    echo $this->partial('/partials/pokemons_list', array('pokemons' => $result['pokemons'], 'types' => $result['types']));
    ?>
<?php endforeach;?>
